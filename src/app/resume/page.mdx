---
title: "Jake's Resume"
---
import { FaGithub } from "react-icons/fa";
import { FaLinkedin } from "react-icons/fa";

export const meta = {
  title: "Jake's Resume",
}

export default function JakeResume({ data }) {
  // fallback sample data — you can replace with your own or pass `data` prop
  const resume = data ?? {
    name: 'Jake Reynolds',
    role: 'Senior Frontend Engineer',
    location: 'Chicago, IL',
    email: 'jake@example.com',
    phone: '(555) 555-5555',
    website: 'jake.dev',
    linkedin: 'linkedin.com/in/jakereynolds',
    github: 'github.com/jakerey',
    summary:
      'Front-end engineer focused on performant, accessible interfaces and delightful UX. Strong React/TypeScript background with a soft spot for design systems.',
    skills: [
      'React',
      'TypeScript',
      'Tailwind CSS',
      'Next.js',
      'Node.js',
      'Accessibility',
      'Testing (Jest, RTL)',
      'CI/CD',
    ],
    experience: [
      {
        company: 'BrightApps',
        title: 'Senior Frontend Engineer',
        start: 'Jul 2021',
        end: 'Present',
        bullets: [
          'Led a team of 3 engineers to build a design system used across 6 products.',
          'Reduced bundle size by 30% and improved Lighthouse performance score from 55 to 92.',
          'Mentored junior engineers and established PR review standards.',
        ],
      },
      {
        company: 'Pixelworks',
        title: 'Frontend Engineer',
        start: 'Jan 2018',
        end: 'Jun 2021',
        bullets: [
          'Implemented a component-driven workflow with Storybook and automated visual tests.',
          'Built accessible form components and reduced support tickets by 18%.',
        ],
      },
    ],
    education: [
      {
        school: 'University of Illinois',
        degree: 'B.S. Computer Science',
        year: '2017',
      },
    ],
    projects: [
      {
        name: 'OpenDash',
        link: 'https://github.com/jakerey/opendash',
        desc: 'An open-source analytics dashboard for small teams.'
      }
    ]
  }

  return (
    <div className="max-w-4xl mx-auto  p-8 md:p-12 rounded-2xl ">
      <header className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
        <div>
          <h1 className="text-3xl md:text-4xl font-extrabold">{resume.name}</h1>
          <p className="text-sm md:text-base  mt-1">{resume.role} — {resume.location}</p>
        </div>
        <div className="flex flex-col md:items-end text-sm ">
          <a href={`mailto:${resume.email}`} className="hover:underline">{resume.email}</a>
          <a href={`tel:${resume.phone}`} className="hover:underline">{resume.phone}</a>
          <a href={`https://${resume.website}`} target="_blank" rel="noreferrer" className="hover:underline">{resume.website}</a>
        </div>
      </header>

      <section className="mt-6 grid grid-cols-1 md:grid-cols-3 gap-6">
        <aside className="md:col-span-1">
          <div className="mb-6">
            <h3 className="text-sm font-semibold uppercase ">Summary</h3>
            <p className="mt-2 text-sm ">{resume.summary}</p>
          </div>

          <div className="mb-6">
            <h3 className="text-sm font-semibold uppercase ">Skills</h3>
            <ul className="mt-2 flex flex-wrap gap-2">
              {resume.skills.map((s) => (
                <li key={s} className="text-xs px-2 py-1 rounded-full border ">{s}</li>
              ))}
            </ul>
          </div>

          <div className="">
            <h3 className="text-sm font-semibold uppercase ">Links</h3>
            <div className="mt-2 text-sm text-accent/60  inline-flex gap-4">
              <a href={`https://${resume.linkedin}`} target="_blank" rel="noreferrer" className="hover:underline"><FaGithub className="size-12 hover:text-accent text-accent-foreground"/></a>
            <a href={`https://${resume.github}`} target="_blank" rel="noreferrer" className="hover:underline"><FaLinkedin className="size-12 hover:text-accent text-accent-foreground"/></a>
            </div>
          </div>
        </aside>

        <main className="md:col-span-2">
          <section>
            <h2 className="text-xl font-semibold">Experience</h2>
            <div className="mt-4 space-y-6">
              {resume.experience.map((exp) => (
                <article key={exp.company} className="p-4 rounded-lg bg-accent dark:bg-accent">
                  <div className="flex items-start justify-between">
                    <div>
                      <h3 className="font-semibold">{exp.title}</h3>
                      <p className="text-sm ">{exp.company}</p>
                    </div>
                    <div className="text-sm ">{exp.start} — {exp.end}</div>
                  </div>
                  <ul className="mt-3 list-disc list-inside ">
                    {exp.bullets.map((b, i) => (<li key={i} className="text-sm">{b}</li>))}
                  </ul>
                </article>
              ))}
            </div>
          </section>

          <section className="mt-8">
            <h2 className="text-xl font-semibold">Projects</h2>
            <div className="mt-4 space-y-4">
              {resume.projects.map((p) => (
                <div key={p.name} className="p-4 rounded-lg ">
                  <div className="flex items-center justify-between">
                    <div>
                      <h3 className="font-semibold">{p.name}</h3>
                      <p className="text-sm ">{p.desc}</p>
                    </div>
                    {p.link && (
                      <a href={p.link} target="_blank" rel="noreferrer" className="text-sm underline">View</a>
                    )}
                  </div>
                </div>
              ))}
            </div>
          </section>

          <section className="mt-8">
            <h2 className="text-xl font-semibold">Education</h2>
            <div className="mt-4 space-y-2">
              {resume.education.map((e) => (
                <div key={e.school} className="p-4 rounded-lg  flex items-center justify-between">
                  <div>
                    <div className="font-semibold">{e.school}</div>
                    <div className="text-sm ">{e.degree}</div>
                  </div>
                  <div className="text-sm ">{e.year}</div>
                </div>
              ))}
            </div>
          </section>
        </main>
      </section>

      <footer className="mt-8 text-xs text-center ">This is a template I plan to edit this later with real info</footer>
    </div>
  )
}
